# JV Web

JV Web is the browser product line for Jozz Vehicle: a driveable desktop/mobile product, practical R&D surface and accumulated evidence corpus.

## Authority and publication

```text
Jozzpoly/JV-Box3D-Web-experiment
  main -> current accepted product/source + canonical documentation authority
  research/* -> scoped research evidence only when current routing names it
  work/* -> bounded unpromoted work/evidence unless explicitly promoted
  preview/owner-control -> exact-source Owner Preview composition infrastructure

Jozzpoly/JV-Box3D-Web-Public
  accepted Friends/public artifact authority
```

Live Git/current files, exact executed evidence and direct scoped Owner observation outrank stale documentation and historical plans.

Do not confuse the current `main` documentation head with a newly validated executable build. The accepted Steering I1 executable snapshot used by canonical Owner Preview remains:

`529ae7d3e6d09faf2cfdd5bb034b01c693f8f9c0`

The current docs consolidation is documentation-only. Product/runtime promotion requires its own evidence and decision.

## Current project mode

The strategic cold-takeover campaign from 2026-08-26 is **closed and historical**. Do not restart it because a new chat, Codex session or executor appears.

JV-Web is in bounded product/R&D iteration:

`real need or uncertainty -> smallest informative change/experiment -> risk-matched validation -> faithful evidence -> Owner judgement when experiential -> next iteration`

JV-Web is its own browser product/R&D surface. It is not NextGen JV Lite and does not need to converge architecturally with NextGen JV.

## Accepted product baseline

Steering I1 remains the accepted product/current-best baseline:

`docs/baselines/STEERING_I1_CURRENT_BEST_2026-09-01.md`

Important accepted truth includes:

- Direct Rotation and Relative-X steering foundations;
- semantic pointer `RELEASE` instead of hidden automatic return-to-zero;
- hands-off graphical steering following the live physical rack;
- re-grab anchored to the current physical rack state;
- 900-degree current default/current-best steering range, with other ranges still candidates;
- artificial centering not exposed as an Owner-facing dependency;
- natural physical steering self-return still unresolved;
- established desktop/mobile controls, camera/fullscreen, accepted responsive composition and scan/render boundaries.

These capabilities are valuable product/reference capital, not a permanent architecture freeze.

Canonical Owner Preview currently remains pinned to:

- executable source: `529ae7d3e6d09faf2cfdd5bb034b01c693f8f9c0`;
- accepted JSPREV2: `Jozzpoly/JV-Box3D-Web-Public@a325c279cfe63a0607dba33c3c635a1716e09f8f`.

## Retained wheel-mode5 research

Current retained branch:

`research/wheel-mode5-rq2c-orientation-2026-09-05`

Current classification:

`RQ2C_ORIENTATION_HOLD`

The zero-degree orientation control failed the frozen actual-support rolling-slip gate. RQ2C4D/E/F localized the residual far enough to establish useful evidence, while exact numerical per-subsystem source ownership remains partial. Planned `+3.5°/-3.5°` yaw was not executed because the zero-degree gate failed.

Routine RQ2C micro-forensics are closed. Wheel-mode5 product integration is not authorized.

When this research is relevant, resolve the branch live and read its current `docs/HANDOFF.md`, active research pointer, wheel roadmap and RQ2C4F closure. Do not expect those branch-local research files to exist on `main`.

## Post-wheel spawn calibration

A separate bounded product investigation exists on:

`work/spawn-landmark-calibration-2026-09-05`

Current classification:

`PARTIAL / OWNER_CALIBRATION_READY / NOT_PROMOTABLE`

It falsified the earlier explanation that ordinary Owner Preview's bad start came from `scanCenterSpawn()`. The relevant accepted source defaulted to `map` / `world.spawn = { x: 0, y: 1.2, z: 0 }`, while accepted JSPREV2 begins around world `z = 320`.

Two geometry methods found several stable broad flat regions in the accepted scan, but geometry cannot establish road semantics. Three spatially separated candidates exist only as opt-in `scan-cal-a/b/c` on the work branch.

Focused spawn tests and typecheck passed. The broader test run exposed seven pre-existing steering/mobile-UI assertions whose relevant blobs are identical to accepted `main`; the run stopped before `build:portable`, so the spawn candidate build remains **UNVERIFIED**.

Do not call any A/B/C candidate a road or promote this branch. If resumed, qualify the candidate build first and then use a bounded Owner A/B/C semantic comparison.

## Start here

For a fresh normal continuation:

1. verify live `main`, `preview/owner-control` when relevant, and every non-main branch actually under discussion;
2. read `AGENTS.md`;
3. read `docs/PROJECT_STATE.md`;
4. read `docs/HANDOFF.md` for an actual continuation/handoff;
5. if wheel-mode5 matters, resolve `research/wheel-mode5-rq2c-orientation-2026-09-05` live and use its current routing/evidence docs;
6. if spawn calibration matters, resolve `work/spawn-landmark-calibration-2026-09-05` live and read `docs/SPAWN_LANDMARK_CALIBRATION_2026-09-05.md` there;
7. inspect only source/tests/evidence relevant to the current Owner need.

Do not reconstruct a strategic cold takeover or replay the entire wheel experiment sequence unless newer live evidence actually contradicts current routing.

## Program evidence sources

- **JV-Web current repo** — accepted browser product/source on `main`, plus explicitly routed research/work evidence.
- **JV_CORE / Native** — sealed donor/research record; not a mandatory architecture or quality ceiling.
- **JURE** — optional authoring/mechanical-truth donor/tool; not mandatory and not frozen as final application/schema.
- **NextGen JV** — separate deeper construction/platform direction; discoveries may transfer, architecture does not synchronize automatically.

## Canonical toolchain

```text
Node 24.16.0
npm >=11.13.0 <12
packageManager npm@11.13.0
TypeScript 7.0.2
Vite 8.1.5
box3d.js 0.0.2
```

Build/test success establishes reproducibility for the exact source tested; it does not by itself establish product acceptance or future architecture.

## Work discipline

- verify repo/ref/head before writes;
- protect accepted runtime/product behavior from unrelated research changes;
- match validation depth to causal blast radius;
- distinguish accepted product truth, branch research evidence and unpromoted candidates;
- preserve negative/failed evidence instead of tuning it away;
- stop microscopic research when further attribution cannot change the next decision;
- do not create branches, wrappers, gates, workflows or schemas merely for process aesthetics;
- do not ask the Owner to judge laboratory metrics without demonstrated experiential consequence.

Third-party notices are in `THIRD_PARTY_NOTICES.md`.
