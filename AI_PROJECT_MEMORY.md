# AI Project Memory — JV Web

Updated: 2026-09-05
Owner: Jozz

## CURRENT MODE

**BOUNDED JV-WEB PRODUCT/R&D ITERATION**

The 2026-08-26 strategic cold-takeover campaign is historical and closed. Do not restart it automatically.

JV-Web is a standalone browser Jozz Vehicle product/R&D surface, not NextGen JV Lite.

Current work is deliberately separated into:

- accepted product/runtime baseline on `main` + exact executable snapshot;
- retained wheel-mode5 research evidence on `research/wheel-mode5-rq2c-orientation-2026-09-05`;
- unpromoted spawn calibration on `work/spawn-landmark-calibration-2026-09-05`.

Neither non-main branch is accepted product truth.

## AUTHORITY

`live Git/current files -> executed evidence from exact source -> direct scoped Owner/device evidence -> current docs -> history/cross-project memory`

`main` is current accepted source/documentation authority after consolidation.

Canonical Owner Preview is exact-source infrastructure, not authority. At the 2026-09-05 grounding it remains pinned to:

- executable source `529ae7d3e6d09faf2cfdd5bb034b01c693f8f9c0`;
- accepted JSPREV2 `Jozzpoly/JV-Box3D-Web-Public@a325c279cfe63a0607dba33c3c635a1716e09f8f`.

A newer docs-only `main` head does not imply a new executable validation.

## ACCEPTED PRODUCT BASELINE

Steering I1 remains accepted current-best. See:

`docs/baselines/STEERING_I1_CURRENT_BEST_2026-09-01.md`

Retain:

- Direct Rotation + Relative-X remain available;
- pointer release uses semantic `RELEASE`, not hidden center command;
- hands-off steering UI follows live physical rack;
- new grabs re-anchor to live rack position;
- 900 degrees is current default/current-best, not permanently final;
- artificial centering is not an Owner-facing product dependency;
- useful natural physical self-return remains unresolved.

No wheel-mode5 or spawn-calibration runtime change is accepted product truth.

## WHEEL-MODE5 RETAINED RESEARCH

Branch:

`research/wheel-mode5-rq2c-orientation-2026-09-05`

Current classification:

`RQ2C_ORIENTATION_HOLD`

The project moved beyond the old RQ0/E2a2 routing:

- RQ0/RQ1/RQ2a/RQ2b executed and qualified within scope;
- RH0 foundation hardening PASS/CLOSED;
- RQ2C stopped at zero-degree before yaw.

Decisive zero-degree evidence:

- actual-support max slip `0.034093857 mm/s`;
- frozen gate `0.002 mm/s`;
- exceedance `17.0469x`;
- RQ2C4D/E/F localized the dominant rolling-pair residual strongly to axle-spin-rate evolution;
- source review identifies contact tangential friction as the unique obvious first-order direct axial-torque path in the aligned apparatus;
- exact numerical per-subsystem spin ownership remains PARTIAL without new solver-level instrumentation.

Planned `+3.5°/-3.5°` yaw is NOT EXECUTED / NOT VALIDATED. Product integration is NOT AUTHORIZED.

Routine RQ2C micro-forensics are closed. Do not reopen merely for completeness.

For exact evidence, resolve the research branch live and read its branch-local router/evidence docs.

## POST-WHEEL SPAWN CALIBRATION

Branch:

`work/spawn-landmark-calibration-2026-09-05`

Classification:

`PARTIAL / OWNER_CALIBRATION_READY / NOT_PROMOTABLE`

Branch-local state record:

`docs/SPAWN_LANDMARK_CALIBRATION_2026-09-05.md`

Retain:

- earlier claim that ordinary Owner Preview's bad start came from `scanCenterSpawn()` was falsified by exact source;
- accepted startup default was `map` / `world.spawn = { x: 0, y: 1.2, z: 0 }`;
- accepted JSPREV2 begins around world `z = 320`, so current interpretation is poor spatial/semantic placement relative to the scan playground, not a proved scan-center collision failure;
- accepted scan metadata has no semantic road/start landmark;
- exact accepted scan tile hashes/sizes and geometry counts were revalidated;
- a primary flat-region analysis plus independent vertex-normal/local-occupancy crosscheck confirm several stable broad flat regions but cannot label roads;
- primary rank #1 failed the independent crosscheck, so primary ranking is not authority;
- three spatially separated crosscheck-passing candidates exist only as opt-in `scan-cal-a/b/c` on the work branch;
- default spawn modes are unchanged; candidate Y is resolved from collision surface at runtime;
- focused spawn tests and typecheck pass;
- broader candidate validation exposed seven pre-existing steering/mobile-UI assertions whose relevant blobs are identical to accepted `main`;
- run stopped before `build:portable`, therefore candidate build is UNVERIFIED.

Do not choose A/B/C algorithmically, call them roads, weaken canonical Preview pinning or promote this branch. If resumed, build qualification comes before bounded Owner semantic judgement.

## BASELINE TEST DEBT

The seven red assertions exposed by the spawn candidate run are provenance-grounded as pre-existing baseline debt, not spawn regressions, because relevant failing source/test blobs are byte-identical to accepted `main`.

Do not repair unrelated steering behavior/tests inside a spawn task just to obtain green CI.

If exact baseline health becomes a decision dependency, run a fresh exact-main check and treat any difference as new evidence.

## CURRENT ROUTING

Do **not** automatically:

- restart strategic cold takeover;
- resume RQ2C yaw or deeper solver ownership;
- reopen recycler/stiffness/240 Hz sweeps;
- tune physics or relax the RQ2C gate to force PASS;
- promote wheel-mode5 to product runtime;
- promote or semantically label spawn A/B/C without build + Owner evidence;
- fix unrelated baseline steering tests as part of spawn;
- turn JV-Web into NextGen JV Lite.

There is no automatic next experiment. Current Owner priority should choose the next work.

## STILL OPEN / NOT VALIDATED

- useful natural physical steering self-return;
- final steering range across conditions/devices;
- representative wheel lateral force / steer-under-load / camber thrust / load transfer / near-limit traction;
- wheel-mode5 product integration;
- RQ2C yaw equivalence and exact per-subsystem spin ownership;
- spawn candidate portable build;
- Owner semantic judgement of spawn A/B/C;
- any selected road/start landmark or spawn promotion;
- broader current product priorities after these bounded lines.

## DEFAULT WORKFLOW

`uncertainty / product need -> smallest informative experiment or change -> causal validation -> faithful evidence -> Owner judgement when experiential -> next iteration`

Stop microscopic research when further attribution cannot change the next decision.

## FRESH CONTINUATION

1. Resolve live `main`, Preview when relevant, and every branch actually under discussion.
2. Read `AGENTS.md`.
3. Read `docs/PROJECT_STATE.md`.
4. Read `docs/HANDOFF.md` for an actual handoff/continuation.
5. If wheel-mode5 matters, resolve `research/wheel-mode5-rq2c-orientation-2026-09-05` live and read branch-local current routing/evidence.
6. If spawn calibration matters, resolve `work/spawn-landmark-calibration-2026-09-05` live and read its branch-local `docs/SPAWN_LANDMARK_CALIBRATION_2026-09-05.md`.
7. Inspect only source/tests/evidence relevant to the current Owner need.
8. Do not resume historical sequence work by inertia.
