# AI Project Memory — JV Web wheel-mode5 research branch

Updated: 2026-09-05
Owner: Jozz

## AUTHORITY FIRST

Accepted JV-Web product/source/documentation authority is always **live**:

`Jozzpoly/JV-Box3D-Web-experiment/main`

Resolve it before making product claims. Do not treat a stored main SHA on this retained research branch as current authority.

At the 2026-09-05 grounding:

- `main` had advanced by a documentation-only routing consolidation;
- canonical accepted Steering I1 executable snapshot remained `529ae7d3e6d09faf2cfdd5bb034b01c693f8f9c0`;
- canonical Owner Preview remained pinned to that exact executable plus accepted JSPREV2 `Jozzpoly/JV-Box3D-Web-Public@a325c279cfe63a0607dba33c3c635a1716e09f8f`.

A docs-only main advance is not a new executable validation.

This branch:

`research/wheel-mode5-rq2c-orientation-2026-09-05`

is retained wheel research evidence, not product truth.

The strategic cold-takeover campaign from 2026-08-26 is historical and closed.

## ACCEPTED PRODUCT BOUNDARY RETAINED HERE

Unless newer live `main` evidence contradicts it, Steering I1 remains accepted current-best:

- Direct Rotation + Relative-X remain available;
- pointer release uses semantic `RELEASE`, not hidden center command;
- hands-off steering UI follows live physical rack;
- re-grab anchors to live rack position;
- 900 degrees is current default/current-best, not permanently final;
- artificial centering is not an Owner-facing dependency;
- useful natural physical self-return remains unresolved.

No wheel-mode5 research result and no spawn-calibration candidate has been promoted into accepted product runtime.

## WHEEL-MODE5 CURRENT TRUTH

Current classification:

`RQ2C_ORIENTATION_HOLD`

Canonical branch-local sources:

- `docs/evidence/WHEEL_MODE5_ACTIVE_RESEARCH_POINTER_2026-09-05.json`;
- `docs/WHEEL_MODE5_CURRENT_STATE_AND_ROADMAP_2026-09-04.md`;
- `docs/WHEEL_MODE5_RQ2C4F_SOURCE_OWNERSHIP_CLOSURE_2026-09-05.md`;
- `docs/evidence/WHEEL_MODE5_CANONICAL_EVIDENCE_LEDGER_2026-09-05.json`.

The project moved beyond old RQ0/E2a2 routing:

- RQ0/RQ1/RQ2a/RQ2b executed and qualified within scope;
- RH0 foundation hardening PASS/CLOSED;
- RQ2C stopped at zero-degree before yaw.

Decisive zero-degree evidence:

- actual-support max slip `0.034093857 mm/s`;
- frozen gate `0.002 mm/s`;
- exceedance `17.0469x`;
- RQ2C4D/E/F localize the dominant rolling-pair residual strongly to axle-spin-rate evolution;
- source review identifies contact tangential friction as the unique obvious first-order direct axial-torque path in the aligned apparatus;
- exact numerical per-subsystem spin ownership remains PARTIAL without new solver-level transient instrumentation.

Planned `+3.5°/-3.5°` yaw is NOT EXECUTED / NOT VALIDATED. Product integration is NOT AUTHORIZED.

Routine RQ2C micro-forensics are closed. Do not reopen merely for completeness, tune physics to force PASS or relax the gate.

## POST-WHEEL PRODUCT ROUTING

A later bounded product audit created a separate branch:

`work/spawn-landmark-calibration-2026-09-05`

Current retained classification:

`PARTIAL / OWNER_CALIBRATION_READY / NOT_PROMOTABLE`

Its branch-local state record is:

`docs/SPAWN_LANDMARK_CALIBRATION_2026-09-05.md`

Retain only these routing-level conclusions here:

- earlier `scanCenterSpawn()` explanation of the ordinary bad Owner start was falsified by exact source;
- accepted default start was `map` / `{ x: 0, y: 1.2, z: 0 }`, while accepted JSPREV2 begins around world `z = 320`;
- scan metadata has no semantic road/start landmark;
- two geometry methods support several stable broad flat regions but cannot label roads;
- three opt-in A/B/C calibration candidates exist only on that work branch;
- focused spawn tests and typecheck pass;
- broader candidate validation exposed seven pre-existing steering/mobile-UI assertions whose relevant blobs match accepted main;
- candidate build remains UNVERIFIED because the run stopped before `build:portable`.

Do not choose A/B/C algorithmically, call them roads, weaken canonical Preview pinning or promote that branch. If resumed, qualify build then use bounded Owner semantic judgement.

## CURRENT ROUTING

Do not automatically:

- restart strategic cold takeover;
- resume RQ2C yaw or solver ownership;
- reopen recycler/stiffness/240 Hz sweeps;
- tune physics or redefine the `0.002 mm/s` gate;
- promote wheel-mode5 to product runtime;
- merge spawn candidate code into this research branch;
- promote or semantically label spawn A/B/C without build + Owner evidence.

There is no automatic next wheel experiment. Current Owner priority should choose the next JV-Web work.

## FRESH CONTINUATION

1. Resolve live `main` first and read main's `AGENTS.md`, `docs/PROJECT_STATE.md` and handoff routing.
2. Return to this branch only if wheel-mode5 evidence is relevant.
3. Use this branch's active pointer/roadmap/RQ2C4F closure/ledger for exact wheel claims.
4. Resolve the spawn work branch separately if spawn calibration is relevant.
5. Do not resume historical sequence work by inertia.
