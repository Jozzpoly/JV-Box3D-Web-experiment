# JV-Web — handoff

Updated: 2026-09-04
Owner: Jozz

Use this file for an actual handoff/continuation. A new chat is not by itself a new takeover campaign.

## 1. Current mode

JV-Web has two deliberately separate live layers:

- **accepted product truth** on `main`;
- **active wheel-mode5 research evidence** on `work/wheel-mode5-e2a-outer-ground-dynamic-2026-09-03`.

Do not treat the research branch as accepted product source and do not treat `main` as if it contained the experimental wheel-mode5 apparatus.

The 2026-08-26 strategic cold-takeover campaign remains historical. JV-Web is a standalone browser product/R&D surface, separate from NextGen JV.

## 2. Fresh continuation order

Always first resolve live Git because exact SHAs may have moved.

Then read:

1. `AGENTS.md`;
2. `docs/PROJECT_STATE.md`;
3. if continuing wheel-mode5: `docs/WHEEL_MODE5_CURRENT_STATE_AND_ROADMAP_2026-09-04.md`;
4. if continuing wheel-mode5: `docs/WHEEL_MODE5_E2A2AJ_REPROJECTION_COMPONENT_DECOMPOSITION_2026-09-04.md`;
5. older E2a2 documents only for a specific claim/apparatus dependency;
6. if product steering is involved: `docs/baselines/STEERING_I1_CURRENT_BEST_2026-09-01.md` and `docs/contracts/STEERING_COMMAND_CONTRACT_PL.md`.

Do not reconstruct the entire experiment sequence unless live evidence conflicts with the current router.

## 3. Accepted product truth

At the 2026-09-04 roadmap refresh, accepted `main` remained:

`5b28cc03d22264010680deb95a04abd04661bc22`

Steering I1 remains the accepted baseline:

- touch release uses semantic `RELEASE` rather than hidden return-to-zero;
- hands-off graphical steering follows the physical rack;
- new grabs re-anchor to live rack position;
- 900 degrees is current default/current-best, not permanently frozen;
- artificial centering is not an accepted product dependency;
- useful natural physical self-return remains unresolved.

No wheel-mode5 research change has been promoted into this accepted product truth.

## 4. Wheel-mode5 research truth

The E2a2 recycler-forensics descent has reached a deliberate stop boundary.

Strong bounded evidence shows that, in the studied flat-support/fixed-road transition rigs:

- actual recycled-manifold shortcut execution is required for the measured amplification;
- separation reprojection is a necessary amplifier;
- trusted fresh-shadow evidence shows roughly `0.118 mm` recycled-vs-fresh separation discrepancy near the validated fixed-road `2 -> 1` transition;
- stale `baseSeparation` and long cache age are not the primary explanations in representative matched samples;
- E2a2aj showed that absolute frame/anchor subterm magnitude cannot responsibly identify a causal culprit because the final reprojection is a small cancellation residual.

Do not interpret this as a global Box3D recycler bug or as evidence that current product wheel behavior is broken.

## 5. Current frontier

The next research question is no longer:

> Which recycler subterm is microscopically responsible?

It is:

> Does the discovered recycler/reprojection discrepancy materially matter in representative annular-wheel rolling/contact behavior?

The next planned stage is **RQ0 — representative steady rolling baseline**.

RQ0 should use:

- true fixed road;
- intended annular wheel geometry;
- dynamic wheel with controlled normal load;
- nonzero friction;
- bounded steady rolling/spin;
- no deliberately forced topology challenge in the first baseline;
- only non-perturbing observational recycler/contact telemetry.

Judge material behavior: drift/energy injection, jitter, contact stability, impulse plausibility and grip discontinuity.

Do not reproduce the old seam `dVy` metric for its own sake.

## 6. Decision structure after RQ0

If RQ0 is trustworthy, move to **RQ1: one controlled topology challenge**.

Then classify:

- representative behavior good -> stop recycler forensics and continue wheel qualification;
- material recycler-linked disturbance -> reopen narrowly scoped mitigation/mechanism work;
- ambiguous/invalid rig -> repair the rig, not the theory.

Do not bundle camber, irregular road, suspension, drivetrain and chassis coupling into the first representative experiment.

## 7. Not yet validated

Still open:

- representative frictional annular rolling;
- drive/brake traction;
- free camber/steer and load transfer;
- suspension/chassis coupling;
- irregular-road and side/inner/bore contact behavior;
- material product significance of the ~`0.12 mm` discrepancy;
- any production recycler mitigation;
- wheel-mode5 integration into product/Owner Preview.

## 8. Git / Preview discipline

- `main` remains accepted product source authority.
- The wheel-mode5 branch is research evidence authority only for executed scoped experiments.
- `preview/owner-control` remains exact-source composition infrastructure, not authority.
- Do not fast-forward the research branch into `main` without a separate product integration decision and appropriate validation.
- Before writes, verify repo/ref/head again.

## 9. Things a new agent must not infer

Do not infer that:

- E2a2 must continue alphabetically into E2a2ak;
- a laboratory recycler discrepancy requires a production patch;
- the research branch already represents current product physics;
- a symmetric `1 -> 2` lab matrix must be completed before representative work;
- wheel-mode5 is already accepted for Owner Preview;
- 900-degree steering or current M6 architecture is final forever;
- a new chat requires another broad takeover campaign.

Current router: **protect `main`, keep E2a2 closed by default, plan/execute RQ0 next.**