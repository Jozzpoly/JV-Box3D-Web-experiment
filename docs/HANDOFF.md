# JV Web — handoff

Updated: 2026-08-14
Status: `NO ACTIVE TRANSACTION`

The previous front-corner/steering transaction is closed for ordinary work. The Friends R1 browser baseline is live and owner-validated on desktop and phone, including the full public JSPREV2 scan.

Fresh continuation:

1. resolve current private `main` and public `release/friends-r1`;
2. read `AGENTS.md`;
3. read `docs/PROJECT_STATE.md`;
4. inspect only code/tests needed for the current slice.

Do not resume `R1-STEER-COUPLING-01` from historical handoff material. Rig/steering geometry work waits for better authored evidence from JURE unless a new scoped question explicitly changes that priority.

Historical transaction details remain recoverable from Git history and cold campaign/checkpoint documents; they are not current operating instructions.
