# JV-Web — wheel research handoff

Updated: 2026-09-05
Owner: Jozz

Use this file when this retained wheel-mode5 research branch is actually relevant. A new chat is not by itself a reason to reopen the branch or restart a takeover campaign.

## 1. Authority boundary

Accepted JV-Web product/source/documentation authority is always **live `main`**:

`Jozzpoly/JV-Box3D-Web-experiment/main`

Resolve it live before making a product claim. Do not treat an old embedded main SHA on this research branch as current authority.

At the 2026-09-05 grounding:

- `main` had advanced by a docs-only routing consolidation;
- canonical accepted Steering I1 executable snapshot remained `529ae7d3e6d09faf2cfdd5bb034b01c693f8f9c0`;
- canonical Owner Preview still composed that exact executable with accepted JSPREV2 `Jozzpoly/JV-Box3D-Web-Public@a325c279cfe63a0607dba33c3c635a1716e09f8f`.

This branch:

`research/wheel-mode5-rq2c-orientation-2026-09-05`

owns retained wheel research evidence and routing only. It is not accepted product source.

A separate post-wheel product calibration exists on:

`work/spawn-landmark-calibration-2026-09-05`

It is also unpromoted and must not be confused with this wheel branch.

The 2026-08-26 strategic cold-takeover campaign remains historical and closed.

## 2. Fresh continuation order

For normal JV-Web continuation:

1. resolve live `main` and read `main`'s `AGENTS.md`, `docs/PROJECT_STATE.md` and handoff;
2. return here only if wheel-mode5 evidence is relevant;
3. on this branch, read `docs/evidence/WHEEL_MODE5_ACTIVE_RESEARCH_POINTER_2026-09-05.json`;
4. read `docs/WHEEL_MODE5_CURRENT_STATE_AND_ROADMAP_2026-09-04.md`;
5. read `docs/WHEEL_MODE5_RQ2C4F_SOURCE_OWNERSHIP_CLOSURE_2026-09-05.md` when exact closure detail matters;
6. use `docs/evidence/WHEEL_MODE5_CANONICAL_EVIDENCE_LEDGER_2026-09-05.json` for exact provenance/evidence;
7. open older E2a2/RQ/RH0 docs only for a specific claim/apparatus dependency;
8. if current spawn-location work is relevant instead, resolve `work/spawn-landmark-calibration-2026-09-05` live and read its `docs/SPAWN_LANDMARK_CALIBRATION_2026-09-05.md`.

Do not reconstruct or replay the entire research sequence unless newer live evidence conflicts with the current router.

## 3. Accepted product boundary retained by this research

Steering I1 remains the accepted executable/current-best product baseline unless newer live `main` evidence says otherwise:

- touch release uses semantic `RELEASE` rather than hidden return-to-zero;
- hands-off graphical steering follows the physical rack;
- new grabs re-anchor to live rack position;
- 900 degrees is current default/current-best, not permanently frozen;
- artificial centering is not an accepted Owner-facing product dependency;
- useful natural physical self-return remains unresolved.

No wheel-mode5 research change and no spawn-calibration candidate has been promoted into accepted product runtime or canonical Owner Preview.

## 4. Wheel-mode5 progression retained as evidence

The old E2a2 recycler-forensics descent is bounded history. It established a real recycler/reprojection discrepancy in the studied transition, including roughly `0.118 mm` recycled-vs-fresh separation in trusted shadow evidence. This does not establish a global Box3D recycler bug or current product failure.

The subsequent representative RQ line reached:

- RQ0 aligned steady rolling — qualified within scope;
- RQ1 bounded road-normal transition — qualified within scope;
- RQ2a braking and RQ2b drive — qualified within scope, with drive/brake sign-asymmetry retained as a sentinel;
- RH0 Research Foundation Hardening — PASS / CLOSED;
- RQ2C orientation qualification — stopped at the zero-degree control before yaw.

Therefore any text saying "RQ0 is next" is historical and must not be followed.

## 5. RQ2C zero-degree closure

The intended yaw challenge remained:

`0° / +3.5° / -3.5°`

with frozen research-instrument budgets:

- max settled axle-axis error `<= 0.035°`;
- max settled velocity-heading error `<= 0.035°`;
- actual-support rolling-slip gate `<= 0.002 mm/s`.

The zero-degree actual-support witness fails the slip gate:

- max witness slip `0.034093857 mm/s`;
- gate exceedance `17.0469x`.

RQ2C4 hard-relax made heading/plane control effectively exact, so the blocker is not ordinary guide drift.

RQ2C4D showed the real support witness retains about `98.28%` of the legacy residual, so fixed-radius bookkeeping is not the primary cause.

RQ2C4E localized the peak primarily to the rolling pair rather than non-spin/nutation.

RQ2C4F localized the peak rolling-pair residual `0.034570694 mm/s` into:

- COM tangential drift `+0.004529953 mm/s`;
- axle-spin-rate contribution `+0.030174406 mm/s`;
- geometric-lever contribution `-0.000109348 mm/s`.

The axle-spin-rate term contributes about `87.3%` of the peak rolling-pair magnitude.

## 6. Source-ownership boundary

Source review of the exact pinned apparatus/solver established contact tangential friction as the unique obvious **first-order direct axial-torque path** in the aligned rolling geometry.

Do not overclaim this result. The existing post-step harness does not close a per-substep/per-subsystem `Delta omega_A` budget. Finite orientation error, inverse-inertia coupling, joint warm-start/solve/relax and gyroscopic integration mean exact numerical causal ownership remains partial without new transient solver-level instrumentation.

Current classification:

`RQ2C4F_SOURCE_OWNERSHIP_PARTIAL_CONTACT_FRICTION_DIRECT`

That instrumentation is deliberately not opened by default because it cannot currently change the project decision.

## 7. Current wheel stop boundary

Routine RQ2C orientation micro-forensics are closed at a truthful HOLD.

- RH0 foundation — **PASS / CLOSED**;
- zero-degree heading/guide behavior — **qualified within apparatus**;
- zero-degree actual-support rolling-slip gate — **FAIL**;
- RQ2C4D/E/F diagnostics — **trusted within scope**;
- exact solver ownership budget — **PARTIAL / not closed**;
- `+3.5°/-3.5°` yaw — **NOT EXECUTED / NOT VALIDATED**;
- wheel-mode5 product integration — **NOT AUTHORIZED**.

This is a valid negative/partial research outcome. Do not execute yaw merely to complete the planned matrix.

## 8. Separate post-wheel spawn audit

A broader product re-ground later selected the Owner-reported poor spawn location as a bounded product problem worth investigating before reopening steering or wheel micro-forensics.

That work is isolated on:

`work/spawn-landmark-calibration-2026-09-05`

Current retained classification:

`PARTIAL / OWNER_CALIBRATION_READY / NOT_PROMOTABLE`

Important findings retained for routing only:

- an earlier interpretation that ordinary Owner Preview spawned via `scanCenterSpawn()` was falsified by exact source; default startup used `map` / `world.spawn`;
- accepted default start was `{ x: 0, y: 1.2, z: 0 }`, while accepted JSPREV2 begins around world `z = 320`;
- accepted JSPREV2 metadata contains no semantic road/start landmark;
- two different geometry methods support several stable broad flat regions but cannot label a road;
- three spatially separated crosscheck-passing candidates exist only as opt-in `scan-cal-a/b/c` on that work branch;
- focused spawn tests and typecheck passed;
- broader candidate validation exposed seven pre-existing steering/mobile-UI assertions whose relevant blobs match accepted `main`;
- the red suite stopped before `build:portable`, so candidate build remains **UNVERIFIED**.

Do not call A/B/C roads, select one algorithmically, weaken canonical Preview pinning or promote the spawn branch. If resumed, qualify build and then use bounded Owner A/B/C semantic judgement.

## 9. Still open / not validated

Wheel/research:

- `+3.5°/-3.5°` orientation equivalence;
- exact numerical per-subsystem ownership of RQ2C4F spin-rate drift;
- dynamic full-annular side/inner/bore contact/manifold semantics;
- representative lateral tire-force behavior;
- steer-under-load handling;
- camber thrust;
- suspension/chassis load transfer;
- near-limit traction/lockup/wheelspin;
- broad rough-road topology;
- production axle/suspension architecture;
- wheel-mode5 product integration and Owner acceptance;
- useful natural physical steering self-return.

Product/spawn:

- candidate portable-build qualification;
- Owner semantic judgement of A/B/C;
- whether any candidate is actually on/near the desired road;
- any product promotion decision for a selected spawn.

## 10. Things a new agent must not infer

Do not infer that:

- a stored `main@...` SHA in historical research docs is live product authority;
- RQ0 is still next;
- E2a2 should continue alphabetically;
- current HOLD should automatically reopen RQ2C;
- solver-level spin ownership must be closed for completeness;
- `+3.5°/-3.5°` yaw should run despite the failed zero-degree gate;
- stiffness/contact parameters or the `0.002 mm/s` gate should be tuned to obtain PASS;
- wheel-mode5 is accepted product runtime;
- spawn A/B/C are roads or a selected default;
- spawn candidate build passed;
- seven steering/UI failures were caused by spawn;
- 900-degree steering or current M6 architecture is final forever;
- a new chat requires another broad takeover campaign.

## 11. Current router

> **Resolve live `main` first. Preserve this wheel branch as trusted bounded evidence and keep RQ2C closed at zero-degree HOLD. Preserve spawn calibration on its separate branch as PARTIAL / OWNER_CALIBRATION_READY / NOT_PROMOTABLE. Do not choose future work by historical sequence inertia.**
